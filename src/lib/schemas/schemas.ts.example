import Schema from '$lib/schemas/lib'; // Load schema constructor

const exampleSchema = new Schema({  // model your data
    title: {
        type: String, // TODO: make possible specifying sample data 'Product title' -> Type string, maxLength: 13ch
        required: true
    },
    text: {
        type: String,
        required: true
    },
    image: String,
    order: Number // TODO: think about sample data
})

export {
    exampleSchema // export schema for later use in config
};
